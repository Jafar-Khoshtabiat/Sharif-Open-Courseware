# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Sharif Open Courseware' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Sharif Open Courseware
      pod 'Moya', '~> 12.0'
      pod 'RealmSwift'
      pod 'SwiftyJSON', '~> 4.0'
      pod 'SwiftSVG', '~> 2.0'

  target 'Sharif Open CoursewareTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Sharif Open CoursewareUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
